
get -init .   # initial current directory

get add .     # update all files and changes to staging area

git config --global user.email "TAnglim@Webucator.com"
git config --global user.name "TAnglim"

get committ -m "Message discribing the changes"

git log      # history of project
git log --author "TAnglim"  # history of author's work

git status   # current state of project

git rm filename  # staged for delete from working & repository

git checkout -- filename #restore filename from repository

git reset HEAD filename  # remove from staging area

git remote add projectName URL      # publish to GitHub URL

git push -u projectName master       # put everything online 



